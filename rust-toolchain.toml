# soroban-sdk 27.x requires Rust >= 1.84.
# `wasm32v1-none` is the correct WASM target for protocol 23+ contracts;
# `wasm32-unknown-unknown` still builds but emits unsupported features.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
targets = ["wasm32v1-none"]
